-- =============================================================================
-- V001__init.sql — Flyway baseline 마이그레이션
--
-- 책임: 본 BFF 의 마이그레이션 베이스라인을 형성하는 자리표시 SQL.
--       실제 테이블 정의는 V002 이후 마이그레이션에서 추가한다.
--
-- 동작:
-- - SELECT 1 한 줄로 마이그레이션 성공 처리만 한다. 부수효과 없음.
--
-- 적용 대상 스키마: user_service
--   (application.yml 의 spring.flyway.schemas / default-schema 가 지정)
--
-- 관련:
-- - V002__schedules.sql 가 schedules 테이블을 생성한다.
-- =============================================================================

-- Flyway baseline 마이그레이션.
-- 본 마이그레이션은 자리표시이며, 실제 테이블 정의는 후속 V002~ 마이그레이션에서 추가한다.
SELECT 1;
