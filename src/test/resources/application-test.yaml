spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS user_service
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        default_schema:   # 메인 yaml의 user_service 오버라이드 — H2는 스키마 불필요

  flyway:
    enabled: false

  data:
    redis:
      host: localhost
      port: 6379

# JWT keys not set → JwtConfig generates ephemeral key pair automatically
jwt:
  private-key:
  public-key:
  access-token-expiry-seconds: 1800
  refresh-token-expiry-seconds: 1209600

# 저장 본문 암호화 키. 값이 소스에 있어도 되는 이유는 테스트가 메모리 데이터베이스만
# 쓰고 그 내용이 프로세스와 함께 사라지기 때문이다. 실행 환경의 변수가 설정 파일보다
# 우선하므로, 셸에 다른 키가 있어도 테스트는 이 값으로 고정된다.
# 서비스 사이로 나가는 좌표를 감싸는 열쇠. 검사에서만 쓰는 고정값이며
# 프로세스와 함께 사라진다. 실행 환경의 변수가 설정 파일보다 우선하므로
# 셸에 다른 값이 있어도 검사는 이 값으로 고정된다.
location:
  crypto:
    enabled: true
    active-kid: test
    keys: test:bWFwLXNlcnZpY2UtdXNlci10ZXN0LWtleS0wMDAwMDA=
  wire:
    enabled: true
    key: bWFwLXdpcmUtdGVzdC1rZXktMDAwMDAwMDAwMDAwMCE=

# 테스터 계정 시더는 테스트에서 돌지 않는다. 환경변수가 설정 파일보다 우선하므로,
# 셸에 TESTER_SEED_ENABLED 가 켜져 있어도 테스트에는 새지 않도록 여기서 못 박는다.
# 켜서 검증하는 테스트는 @SpringBootTest(properties=...) 로 인라인 지정한다 —
# 그 우선순위가 이 파일보다 높다.
tester:
  seed:
    enabled: false

# 내보내기 러너도 테스트에서 돌지 않는다. 셸 환경변수가 새지 않게 못 박는다.
training:
  export:
    enabled: false

kakao:
  client-id: test-client-id
  client-secret: test-client-secret
  redirect-uri: https://test.example.com/api/v1/auth/kakao/callback
  authorize-uri: https://kauth.kakao.com/oauth/authorize
  app-callback-scheme: mapauth://kakao
  token-uri: https://kauth.kakao.com/oauth/token
  user-info-uri: https://kapi.kakao.com/v2/user/me

# 날씨 감시 배치는 테스트에서 돌지 않는다. 컨텍스트를 띄우는 테스트가 최초 지연을
# 넘겨 살아 있으면 배치가 깨어나 hub 를 부르는데, 시험 환경에는 hub 가 없다.
# 지금은 지연(60초)이 테스트 수명보다 길어 우연히 안 터질 뿐이라, 느린 기기나
# CI 에서만 실패하는 종류의 불안정을 남기지 않으려면 여기서 꺼 둔다.
# 감시가 무엇을 고르는지는 ScheduleWeatherWatcherTest 가 빈을 직접 만들어 본다.
weather-watch:
  enabled: false
